{
  "name": "gestion-hotel-rules-tests",
  "private": true,
  "devDependencies": {
    "@firebase/rules-unit-testing": "^2.0.4",
    "firebase-tools": "^13.16.0",
    "firebase-admin": "^11.11.0",
    "mocha": "^10.4.0"
  },
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-project \"mocha firestore.rules.spec.js\""
  }
}
